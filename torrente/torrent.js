
(function crearNodoPar() {
  let config = require("./inicialConfig.json");
  let nodoPar = {
    ant: config.direcciones.ant,
    sig: config.direcciones.sig,
    diccionario: new Map(),
    id: config.id,
  };

  
})





